using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace tinderbot.Parser
{
    /// <summary>
    /// This class will handle the session of the conversation, by implementing a simple protocol that will allow the app decide when the credential have to be asked
    /// among other things like.
    /// 1) Command to list the contacts that the bot is talking with
    /// 2) Where the contact is in the talk until it is delegated to the proper user.
    /// 
    /// </summary>
    public class BotParser
    {
    }
}